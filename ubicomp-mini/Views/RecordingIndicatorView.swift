import SwiftUI
import AppKit

// The recording indicator (pulsing dot + timer + Stop button) lives inside the
// RecordingToolbarController panel — its contents are swapped in place after
// the user hits Record. See RecordingActiveToolbarView in
// RecordingToolbarWindow.swift. This file is kept as a placeholder.
